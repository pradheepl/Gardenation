@model IEnumerable<GardenationApp.Models.PromptListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TriggerDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Garden.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PromptListType.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TriggerDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Garden.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PromptListType.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PromptListItemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PromptListItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PromptListItemID })
        </td>
    </tr>
}

</table>
