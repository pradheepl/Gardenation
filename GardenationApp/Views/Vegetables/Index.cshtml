@model IEnumerable<GardenationApp.Models.Vegetable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LastWateredDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HarvestDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SowDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Garden.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VegetableType.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LastWateredDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HarvestDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SowDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Garden.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VegetableType.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VegetableID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VegetableID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VegetableID })
        </td>
    </tr>
}

</table>
